# Create a file called render.yaml in your server folder
services:
  - type: web
    name: extension-auth-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 18.17.1